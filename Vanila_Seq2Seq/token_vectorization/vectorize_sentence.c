#include "include.h"

void vectorize_sentences(const wchar_t *sentence, int **sentence_vector, const wchar_t **vocab_array, const int vocab_size)
{
    if (sentence == NULL || sentence_vector == NULL
}
